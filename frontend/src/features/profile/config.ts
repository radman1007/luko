/**
 * پیکربندی «کارگاه لوکوریان‌سازی» — همه‌ی آستانه‌ها و قیمت‌ها اینجاست.
 * برای تغییر سختی/قیمت فقط همین فایل را ویرایش کنید؛ هیچ عدد دیگری در کد نیست.
 * TODO: بعد از آماده شدن بک‌اند، این مقادیر از پنل ادمین خوانده می‌شوند.
 */

/** شرط‌های باز شدن کارگاه — همه باید برقرار باشند */
export const BUILDER_UNLOCK = {
  /** حداقل سکه‌ی معمولی */
  coins: 500,
  /** حداقل کل مأموریت‌های انجام‌شده (کلاب + ورزش) */
  totalTasks: 40,
  /** حداقل روزهای پیاپی فعالیت */
  streak: 7,
  /** حداقل بازی‌های آموزشی تمام‌شده */
  gamesCompleted: 15,
} as const;

/**
 * «گنج گمشده‌ی شهر لوکو» — ارز ویژه‌ی کارگاه.
 * با سکه‌ی معمولی قابل خرید نیست؛ فقط از دستاوردهای بزرگ به دست می‌آید.
 */
export const TREASURE = {
  name: 'گنج',
  /** گنجِ جایزه‌ی هر دستاورد */
  rewards: {
    medalUnlocked: 1,
    weekStreakCompleted: 2,
    bookCompleted: 3,
  },
} as const;

/** قیمت آیتم‌های ظاهری کاراکتر — به گنج */
export const BUILDER_PRICES = {
  skinColor: 2,
  outfit: 3,
  hair: 2,
  face: 4,
  name: 1,
  accessory: 3,
} as const;

/**
 * ساخت کاراکتر اختصاصی از روی عکس خود بچه (مرحله‌ی آخر) —
 * تیم لوکو تا یک هفته کاراکتر را می‌سازد. عمداً خیلی گران.
 */
export const CUSTOM_CHARACTER = {
  priceTreasure: 60,
  deliveryDays: 7,
} as const;
